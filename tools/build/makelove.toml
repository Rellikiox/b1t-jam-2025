name = "survive-the-beat"
default_targets = ["win32", "macos", "lovejs"]
build_directory = "../builds"
love_files = [
    "+*",
    "-*/.*",
]

[hooks]
postbuild = [
	"../tools/build/deploy.sh",
]
